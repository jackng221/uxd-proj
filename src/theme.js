import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#fdddff',
      dark: '#caabcc'
    },
    secondary:{
        main: '#f5f5f5',
        dark: '#c2c2c2'
    },
    text:{
        main: '#000000',
    },
    search:{
        main: '#ffb6e4',
    }
  },
});
