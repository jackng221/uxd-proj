import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#fdddff',
      dark: '#caabcc'
    },
    secondary:{
        main: '#ededed',
        dark: '#bbbbbb'
    },
    text:{
        main: '#000000',
    },
    search:{
        main: '#ffb6e4',
    }
  },
});
